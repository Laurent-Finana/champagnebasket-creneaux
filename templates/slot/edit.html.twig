{% extends 'base.html.twig' %}

{% block title %}Modifier un créneau
{% endblock %}

{% block body %}
	<div class="my-10 max-w-md mx-auto">
		<h1 class="mb-8 text-2xl text-center">Modifier le créneau</h1>
		{{ include('slot/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

		<a href="{{ path('app_slot_index') }}" class="block text-sm text-center p-2 border rounded-lg bg-blue-400 text-black hover:border-transparent hover:bg-blue-300">Retour à la liste</a>

		{{ include('slot/_delete_form.html.twig') }}
	</div>
{% endblock %}
